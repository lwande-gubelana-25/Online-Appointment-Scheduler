from setuptools import setup, find_packages

setup(
    name='online_appointment_scheduler',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    description='A basic appointment scheduler system.',
    author='Your Name',
    author_email='you@example.com',
)
